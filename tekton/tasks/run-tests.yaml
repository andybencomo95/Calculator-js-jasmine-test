apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: run-tests
spec:
  workspaces:
    - name: source
  steps:
    - name: test
      image: node:18-alpine
      script: |
        cd $(workspaces.source.path)
        npm ci
        npm test
